import { Button } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <div>
      Home return <Button variant="contained">Hello World</Button>;
    </div>
  );
};

export default Home;
